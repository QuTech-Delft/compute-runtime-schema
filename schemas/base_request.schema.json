{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Base request",
  "description": "The message template with the most basic information needed for a standalone request.",
  "type": "object",
  "properties": {
    "command": {
      "description": "A string identifying the function to be executed.",
      "type": "string"
    },
    "version": {
      "description": "String containing the version number of the message format.",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d$"
    }
  },
  "required": [
    "command",
    "version"
  ]
}