{
  "title": "Trigger publish request",
  "description": "A request from 2200 to 2300 to publish the dynamic information again.",
  "type": "object",
  "allOf": [
    {
      "$ref": "../base_request.schema.json"
    }
  ],
  "properties": {
    "command": {
      "const": "trigger_publish"
    }
  }
}
