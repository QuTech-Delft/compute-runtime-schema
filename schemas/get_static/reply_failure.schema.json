{
  "description": "Reply for when hardware data request fails.",
  "properties": {
    "version": {
      "pattern": "^\\d+\\.\\d+\\.\\d$",
      "title": "Version",
      "type": "string"
    },
    "status": {
      "const": "failure",
      "enum": [
        "failure"
      ],
      "title": "Status",
      "type": "string"
    }
  },
  "required": [
    "version",
    "status"
  ],
  "title": "Hardware data failure",
  "type": "object"
}